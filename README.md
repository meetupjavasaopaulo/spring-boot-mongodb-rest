# spring-boot-mongodb-rest
Spring Boot MongoDB ReST 

pré-requisitos: instalação padrão do MongoDB

Subir micro serviço:

```bash
mvn spring-boot:run
```

Criar um pacote executável:

```bash
mvn package
```
